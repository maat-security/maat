# Maat Documentation (English)

This directory is the source of truth for Maat's documentation.
Translations live in `/docs/{locale}` (see `/docs/es` for Spanish) and
are community-contributed via pull request, reviewed by a native
speaker before merge.

Content coming as the product moves past Phase 0.
