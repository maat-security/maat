[pytest]
# src/ modules import each other as top-level names (e.g. "import
# graph", not "from src import graph") — main.py achieves that at
# runtime with a sys.path.insert(). This does the equivalent for the
# test suite without needing a conftest.py sys.path hack.
pythonpath = src
testpaths = tests
